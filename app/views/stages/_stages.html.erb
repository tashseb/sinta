<div class="container">
  <div class="stages-card">
    <ul>
      <% @role.stages.each do |stage| %>
        <li>
          <p><%= stage.name %></p>
        </li>
      <% end %>
    </ul>
  </div>
  <div data-controller="stage">
    <div class="stages-btn">
      <button type="button" class="btn btn-primary">Add</button>
    </div>
    <div id="stage-form">
      <%= simple_form_for [@role, @stage] do |f| %>
        <%= f.input :name %>
        <%= f.submit %>
      <% end %>
    </div>
  </div>
</div>
